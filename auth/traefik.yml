api:
  dashboard: true
  insecure: true

providers:
  file:
    filename: /etc/traefik/dynamic.yml
    watch: true                         # Reload routes if dynamic.yml changes

entryPoints:
  web:
    address: ":80"

log:
  level: INFO
accessLog: {}
