# deploy/helm/aispm/templates/grafana-deployment.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data
  namespace: {{ .Values.global.namespace }}
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: {{ .Values.grafana.storageClass }}
  resources:
    requests:
      storage: {{ .Values.grafana.storage }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
  namespace: {{ .Values.global.namespace }}
spec:
  # KNOWN SPOF — single replica because Grafana defaults to a SQLite
  # backing store on a RWO PVC.  Pod loss = ~30s dashboard outage
  # while kubelet reschedules.  Stateless from the user's perspective
  # (sessions reissue cleanly), so the impact is operator UX, not
  # data loss.
  #
  # True HA requires backing Grafana with the CNPG Postgres cluster
  # (GF_DATABASE_TYPE=postgres) and removing the PVC, then bumping
  # replicas: 2 + sessionAffinity: ClientIP on the Service.  See
  # follow-up "move Grafana to CNPG-backed HA".
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels: { app: grafana }
  template:
    metadata:
      labels: { app: grafana }
    spec:
      containers:
        - name: grafana
          image: grafana/grafana:11.4.0
          ports:
            - containerPort: 3000
          env:
            - name: GF_SECURITY_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: GRAFANA_ADMIN_PASSWORD
            - name: GF_AUTH_ANONYMOUS_ENABLED
              value: "false"
          readinessProbe:
            httpGet: { path: /api/health, port: 3000 }
            initialDelaySeconds: 10
            periodSeconds: 10
          volumeMounts:
            - name: provisioning
              mountPath: /etc/grafana/provisioning/datasources
              subPath: datasources_datasources.yaml
            - name: provisioning
              mountPath: /etc/grafana/provisioning/dashboards
              subPath: dashboards_dashboards.yaml
            - name: dashboards
              mountPath: /var/lib/grafana/dashboards
            - name: data
              mountPath: /var/lib/grafana
      volumes:
        - name: provisioning
          configMap:
            name: grafana-provisioning
        - name: dashboards
          configMap:
            name: grafana-dashboards
        - name: data
          persistentVolumeClaim:
            claimName: grafana-data
