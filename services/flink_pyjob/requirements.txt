# setuptools pinned <81 because apache-beam (pulled transitively by
# apache-flink) calls `import pkg_resources` from its setup.py.
# Setuptools 81+ removed pkg_resources from the default install, breaking
# wheel builds in fresh venvs (pip-audit, CI). Symptom:
#   ModuleNotFoundError: No module named 'pkg_resources'
# Remove this pin once apache-beam migrates off pkg_resources.
setuptools<81

apache-flink>=1.20.4
# Transitive deps of platform_shared (imported by detection.py + state.py
# via the audit emitter and risk engine):
#   pydantic        — platform_shared/models.py defines AuditEvent etc.
#   kafka-python-ng — platform_shared/kafka_utils.py + audit.py producer
# boto3 stays for future S3 checkpoint backend (multi-host); harmless idle.
pydantic>=2.11.0
boto3==1.34.0
kafka-python-ng==2.2.3

# ─── Transitive CVE fixes — auto-generated 2026-05-07 ────────────────────────────
# Source: deploy/audit-reports/latest/osv-scanner/services_flink_pyjob_requirements.txt.json
# These pins override transitive resolutions to bring vulnerable packages
# forward to a fixed version. Re-generate after each audit cycle.
orjson>=3.11.6  # was 3.9.9; fixes: GHSA-hx9q-6w63-j58v, GHSA-pwr2-4v36-6qpr, PYSEC-2024-40
pyjwt>=2.12.0  # was 2.9.0; fixes: GHSA-752w-5fwx-jx9f
pytest>=9.0.3  # was 8.3.3; fixes: GHSA-6w46-j5rx-g56g
setuptools>=78.1.1  # was 9.1.0; fixes: GHSA-5rjg-fvgr-3xxf, GHSA-cx63-2mw6-8hw5, GHSA-r9hx-vwmv-q579, PYSEC-2022-43012, PYSEC-2025-49
urllib3>=2.6.3  # was 1.26.9; fixes: GHSA-2xpw-w6gg-jr37, GHSA-34jh-p97f-mpxf, GHSA-38jv-5279-wg99, GHSA-g4mx-q9vg-27p4, GHSA-gm62-xv2j-4w53, GHSA-pq67-6m6q-mj2v, GHSA-v845-jxx5-vc9f, PYSEC-2023-192, PYSEC-2023-212

