# Orchestrator calls go through the Vite proxy (/api/v1 → localhost:8094)
# Do NOT set VITE_ORCHESTRATOR_URL to an absolute http:// address — that sends
# requests directly from the browser, which cannot reach the sandbox port.
# VITE_ORCHESTRATOR_URL=http://localhost:8094/api/v1   ← removed intentionally
